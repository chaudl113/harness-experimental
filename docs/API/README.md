# API Documentation

No API is currently defined. This directory will host:

- API contract specifications (OpenAPI, GraphQL schema, gRPC proto)
- Request/response examples
- Authentication and authorization patterns
- Rate limiting and error handling conventions

## When to populate

Populate when the first API surface is defined during implementation. Prefer machine-readable formats (OpenAPI YAML, `.proto`, `.graphql`) over prose where possible.
